import { ListMoneyFormatPipe } from './list-money-format.pipe';

describe('ListMoneyFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ListMoneyFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
