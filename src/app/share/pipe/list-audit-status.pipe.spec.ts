import { ListAuditStatusPipe } from './list-audit-status.pipe';

describe('ListAuditStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ListAuditStatusPipe();
    expect(pipe).toBeTruthy();
  });
});

