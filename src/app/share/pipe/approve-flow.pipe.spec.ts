import { ApproveFlowPipe } from './approve-flow.pipe';

describe('ApproveFlowPipe', () => {
  it('create an instance', () => {
    const pipe = new ApproveFlowPipe();
    expect(pipe).toBeTruthy();
  });
});
