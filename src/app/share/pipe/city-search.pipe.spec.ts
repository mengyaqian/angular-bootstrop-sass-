import { CitySearchPipe } from './city-search.pipe';

describe('CitySearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CitySearchPipe();
    expect(pipe).toBeTruthy();
  });
});
