import { NumberToCharPipe } from './number-to-char.pipe';

describe('NumberToCharPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberToCharPipe();
    expect(pipe).toBeTruthy();
  });
});
