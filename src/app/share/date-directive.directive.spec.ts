import { DateDirectiveDirective } from './date-directive.directive';

describe('DateDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DateDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
